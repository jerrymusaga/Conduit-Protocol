# Foundry
out/
cache/
broadcast/
!broadcast/*/dry-run/

# Dependencies — Foundry installs submodules into the repo-root lib/.
# Anchored with a leading slash so it does NOT also ignore conduit-dapp/lib/.
/lib/

# Env
.env
.env.local

# OS
.DS_Store

# Editors
.vscode/
.idea/

# facilitator
conduit-facilitator/node_modules/
conduit-facilitator/dist/
conduit-facilitator/.env

# endpoint
conduit-endpoint/node_modules
conduit-endpoint/.env

CONDUIT-HANDOFF.md
# stray Next build artifacts in the (non-Next) endpoint service
conduit-endpoint/.next/

# Pitch + demo working materials (not part of the shipped product)
pitch-demo/
